1. When fetching more events, there is no need to keep previous events records since they are already rendered. Instead, keep the only the currently fetched events. - Need to change the Render function.

2. Once all events are fetched and the user is viewing the last event card, the height of the vertical timeline line should be set to 100%. 